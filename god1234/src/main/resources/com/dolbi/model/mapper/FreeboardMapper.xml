<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dolbi.model.mapper.FreeboardMapper"><!-- 클래스 또는 인터페이스 역할 -->
	
	<sql id="FreeboardColumns">
		F_No,F_TITLE,F_CONTENT,F_REGDATE,F_READCOUNT,F_DELETED,F_DEADLINE,
		F_TYPE,M_ID
		FROM FREEBOARD
	</sql>
	
	<!-- <resultMap type="com.mvcdemoweb.model.dto.Member" id="memberMap"> -->
<!-- 	<resultMap type="Freeboard" id="FreeboardMap">
		<id property="J_NO" column="J_NO" />
		<result property="email" column="email" />
		<result property="userType" column="userType" />
		<result property="active" column="active" />
		<result property="J_regDate" column="J_regDate" />
	</resultMap> -->
	<!-- <select id="selectMembers2" resultMap="memberMap">
		SELECT memberId, email, userType, active, regDate 
		FROM JOBBOARD
	</select> -->
	
	<!-- 메서드 역할 -->
	<select id="selectFreeboards" resultType="Freeboard">
		SELECT <include refid="FreeboardColumns" /> 
		FROM FREEBOARD
	</select>
	
	<!-- <select id="selectMemberByIdAndPasswd" 
		parameterType="hashMap" resultType="Jobboard">
		SELECT <include refid="MemberColumns" /> 
		FROM JOBBOARD
		WHERE memberId = #{M_ID} AND M_PW = #{M_PW} AND active = true
	</select>
	
	<select id="selectMemberById" 
		parameterType="String" resultType="Member">
		SELECT <include refid="memberColumns" /> 
		FROM JOBBOARD
		WHERE M_ID = #{M_ID}
	</select> -->
	
	<insert id="insertFreeboard" parameterType="Freeboard">
		INSERT INTO FREEBOARD (freeboardNo, freeboardTitle, freeboardContent, freeboardRegDate, freeboardReadCount, freeboardDeleted,freeboardtype, 
		 memberId)
		VALUES (#{freeboardNo}, #{freeboardTitle}, #{freeboardContent}, #{jobboardRegDate}, #{jobboardReadCount},#{freeboardDeleted}
		,#{freeboardDeadLine}#{freeboardtype},#{memberId})
	</insert>
	
</mapper>






